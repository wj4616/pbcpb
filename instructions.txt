1. Gather your session history — every conversation where you built, debugged,
or designed a JUCE VST plugin with AI assistance. Export as text/markdown.
2. Paste this entire prompt as the system prompt or first message.
3. Attach or paste your session history after the prompt.
4. The AI will analyze your sessions and produce a hyper-detailed playbook
customized to YOUR actual workflow, mistakes, and solutions.

For best results: run this on your longest/most complete plugin build session
first, then feed additional sessions to refine. Each run produces a better
playbook because it has more of YOUR data.

TOKEN MANAGEMENT: If your session history is too long for one context window,
split it by phase (DSP work, GUI work, debugging, etc.) and run this prompt
once per chunk. Then do a final run with all the chunk outputs combined to
produce the unified playbook.

Analyze the session history I provide below — raw conversations where I built JUCE/C++ VST audio plugins with AI assistance. Extract every repeated workflow pattern, every error/crash/bug (with symptom, root cause, fix, and prevention rule), every JUCE API pattern and class I used, every DSP algorithm I implemented (with parameters, sample rate handling, CPU cost), every creative decision point where I stopped to listen or evaluate visually, and every session boundary where I started fresh and what context I restated. Group all of this into logical development phases based on the order I ACTUALLY worked — not a textbook order. Each phase gets a gate with conditions that must be true before advancing, blocker examples extracted from times I got stuck, and explicit marking of task ownership: "human_only" for creative/sound decisions I must control, "ai" for implementation the AI handles, "human" for things I do with AI assistance.

Output a single JSON playbook containing: phase checklists with per-task instructions, JUCE class references, common errors, and validation steps; a juce_patterns section cataloging every reusable JUCE API pattern from my sessions; a dsp_catalog of every DSP block I've built with algorithm, implementation, and known issues; a failure_modes table of every bug I hit indexed by phase with prevention rules that become checklist items; a session_management section defining when to start fresh sessions, what context to carry forward, and what to drop; and a cost_optimization section with context window budget allocation. After the playbook, provide a gap analysis listing phases with thin coverage, missing validation steps, and specific questions to ask me to fill holes. Cite which session each extraction came from. If a section has no evidence in my history, include it as a skeleton marked "no session data — populate after next build."

For every phase gate in the playbook, define the exact handoff: what artifact the phase produces (spec doc, architecture doc, task list, code files, test results), what subset of that artifact the NEXT phase needs as input context, and what gets dropped so the context window isn't wasted on stale history. Map each phase to the superpowers skill that drives it — brainstorming for concept/spec phases where the AI interviews me, writing-plans for architecture/design phases where the AI converts my spec into a buildable plan and ordered task list, executing-plans for implementation phases where the AI writes code one task at a time from the plan. Include a phase_handoff field in each phase defining: output_artifacts (files this phase produces), next_phase_context (exactly what to paste into the next session — spec + architecture + current task, not full history), drop (what NOT to carry — resolved debugging threads, rejected approaches, exploratory tangents), and skill (which superpowers skill to load). The handoff protocol is the exit side of each phase transition — it defines what leaves.

The entry side is the agent compilation manifest. For every phase in the playbook, include a compilation field that defines how to assemble a purpose-built agent instance before the phase runs: role (which mindset — Researcher is exploratory and broad, Architect is structural and systematic, Implementer is precise and literal, Reviewer is adversarial and skeptical, Approver evaluates against requirements), personality (communication style, risk tolerance, creativity level tuned to the phase), tools (which tools and capabilities are active — disable what's irrelevant so the agent doesn't reach for the wrong thing), kb_layers (which knowledge base layers load and in what priority — never the full KB, only the slice this phase needs), reference_data (specific artifacts from prior phases piped in via the handoff), system_prompt_additions (phase-specific instructions, constraints, and anti-patterns appended to the base prompt), and success_criteria (what this compiled agent must produce before the phase gate runs). Extract these from my session history: what tools I actually used per phase, what context I restated at session starts, what role I was implicitly asking the AI to play, and where role confusion caused problems. The compilation manifest is what makes single-agent execution competitive with multi-agent — instead of permanent specialists you get a reconstructed specialist per phase, loaded with exactly the context, tools, and behavioral profile it needs and nothing else.
